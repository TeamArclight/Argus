# Contracts

This directory is the shared contract boundary between frontend, backend, and intelligence services.

- `openapi.json` will be generated from FastAPI in later iterations.
- `examples/` contains canonical JSON payload examples for alignment across teams.
